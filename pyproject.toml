[project]
name = "labgroupassigner"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Kevin Middleton", email = "middletonk@missouri.edu" }
]
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "numpy>=2.5.2",
    "pandas>=2.3.0",
    "scipy>=1.18.1",
    "pytest>=9.1.1",
    "shiny",
    "shinylive",
]

[project.scripts]
labgroupassigner = "labgroupassigner:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
